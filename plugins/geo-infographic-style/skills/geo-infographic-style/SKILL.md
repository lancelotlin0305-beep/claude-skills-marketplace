---
name: geo-infographic-style
description: 依「多色群組卡 × 淺色分層 × 擬 3D」統一風格,將結構化資料繪製成簡報級資訊圖表(SVG 繪製、轉出高解析 PNG)。只要使用者提供結構化資料並希望視覺化——包含系統/功能/角色架構圖、發展藍圖、路線圖、流程圖、分層架構、服務清單、方案比較,或統計數據、問卷結果、營運成果、KPI 佔比等數據視覺化,或說「畫圖」「重繪」「做成圖表」「視覺化」——就必須使用本 skill;使用者上傳一張既有圖表要求依同風格重繪時也適用。此外,在產製簡報、proposal、報告(pptx/文件)過程中,凡遇到需要圖表頁或架構圖的內容,應主動呼叫本 skill 生成圖表後再嵌入,與 pptx 等文件 skill 搭配使用。產圖前會確認 1–5 級畫面豐富度(線框/立體標準/加裝飾/素材嵌入/全場景),4–5 級以使用者提供的外部素材庫嵌入擬真元素。
---

# 資訊圖表繪製(統一風格)

以固定視覺語言把任何結構化資料繪成簡報可讀的資訊圖表。三原則:**一個群組一張卡、一個專色**(顏色即身分);**全圖淺色**(白底,大面積只用淺色漸層,深色僅限標題列、膠囊、徽章、文字、邊框等小元素);**文字載資訊、插圖做質感**(文案逐字忠於原始資料,插圖拿掉後資訊仍完整)。

## 工作流程

1. **確認豐富度等級**:使用者已指定(「用第 N 級畫」)則直接採用;未指定時**先詢問**——1 線框/2 立體標準/3 加裝飾/4 素材嵌入/5 全場景,附情境建議:內容頁 2、海報 3、重點頁 4、封面 5(規格 §13)。選 4/5 級時同時請使用者指定外部素材目錄或上傳所需去背素材(規格 §5),缺件開「元素需求單」。
2. **判斷版型**:依資料結構對應下表。
3. **讀取規格**:繪製前必讀 `references/style-spec.md`(全部規則的唯一出處:色彩、立體、元件、連接、插圖、字級、數據語彙、KPI 列、版型細則、頁面模式、易讀原則、裝飾層)。
4. **先文後框**:逐卡斷行(全形 19–21 字/行、填滿行寬),量出最寬行推得卡寬與卡高;再依用途選頁面模式定畫布——預設自適應(寬 1600–1720)、簡報單頁 16:9(1920×1080)、報告文件 A4 直式(1240×1754),超載一律拆圖不縮字(規格 §10)。
5. **繪製 SVG**:單一檔案;defs 直接取用 `assets/defs.svg` 的漸層/濾鏡/symbol,重複元素用 `<use>`;4/5 級素材以 `<image>` 嵌入(規格 §5)。
6. **轉檔**:`node scripts/render.js 圖.svg` 產出 2 倍解析度 PNG。
7. **自檢**:逐項核對下方檢核清單,不過就修正重轉。
8. **交付/嵌入**:單獨繪圖時交付 PNG+SVG;在簡報/proposal 流程中則將 PNG 嵌入該頁,SVG 一併附上供後續修改。

## 版型判斷

| 資料結構 | 版型 | 佈局 |
|---|---|---|
| 一個核心 + 多個關係群組 | 中心輻射 | 核心卡(淺色)置中,群組卡分列左右,虛線相連 |
| 一個主體 + 3–8 個並列面向 | 環形放射 | 核心圓置中,類別沿環排列,說明卡放射於外圈 |
| 有先後順序的階段 | 流程 | 上升軸線(預設)/山形折線/箭頭鏈;共通基礎層以全寬淺色橫帶壓底 |
| 首尾相接的循環流程 | 循環 | 節點沿圓周排列,主色弧線箭頭順時針串接 |
| 並列分類/層級橫帶 | 分組並列 | 全寬橫帶逐層堆疊,或卡片網格;不畫連接線 |
| 主從/上下階層 | 階層 | 金字塔(3–5 層)或樹狀(直角連線) |
| 空間/場域主題 + 多點說明 | 標註 | 中央情境示意 + 編號圓徽 + 引線 + 說明卡 |
| 2–3 方案對照 | 比較 | 等寬卡並排,項目逐列對齊 |
| 統計/佔比/問卷數據 | 數據 | 人形比例圖、百分比橫條、單位方塊、圓餅/甜甜圈(規格 §7) |

數據語彙(§7)與 KPI 大數字列(§8)可與**任何版型**疊加(如環形+KPI、卡內迷你趨勢);各版型細則見規格 §9。

每卡項目 2–5 條,超過就拆卡;敘述型內容保持段落,不硬轉清單。

## 強制規則(違反即重做)

- **一致性**:同圖同層級元素 = 同版式——卡片等寬、間距相等、內距行距相同;標題列一律「左側徽章 + 標題 + 灰色說明」靠左排。群組差異用顏色表達,不用版面結構表達。
- **留白**:卡寬由內容決定;右內距 ≤ 左內距;文字右緣與卡框間不得超過一個字寬空白;末行 < 1/3 行寬時重新斷行。
- **淺色**:禁止深色大面積底色;任何色塊不得單色平塗,一律漸層(+陰影)。
- **字級**:簡報模式,內文最小 16px、全圖最小字 14.5px(膠囊)。
- **狀態語意**:預留/後續年度項目 = 虛線框 + 灰色時鐘 + 灰字、不打勾;圖底附圖例。
- **內容忠實**:文案逐字沿用原始資料,不增刪、不改寫(僅統一標點);資料中沒有的關係不畫。重繪既有圖表前,先逐項清點原圖所有標籤**與所有連接線(起訖對象、方向、數量)**,產出後逐項回對確認無漏、無擅自增寫、無擅自改接;雙語資料須雙語呈現。內容點不足時以圖示補位,不得湊字(規格 §3)。
- **數據呈現**:統計/佔比資料依規格 §7 的數據語彙與輪色規則呈現。
- **易讀**:遵守規格 §11 三原則(單圖單訊息、直標於圖上、數字帶單位)。
- **連接**:依規格 §4 連接語彙(含 ≥ 6px 淨空與落點規則、箭頭色規)。

## 交付前檢核

- [ ] 文案逐字一致、無遺漏、無擅自增寫;雙語成對;重繪已逐項回對原圖標籤與連接關係(起訖、方向)
- [ ] 文字貼框、無鋸齒短行、無溢出
- [ ] 同層級同版式、等寬等距、字級 ≥ 下限
- [ ] 無深色大面積、色塊皆漸層
- [ ] 預留=虛線+時鐘+灰字,底部有圖例
- [ ] 箭頭/連線與卡片零重疊;插圖光源一致(左上)
- [ ] 立體層次完整:主卡雙層陰影+厚度邊、深色列三段漸層、子卡/項目列/徽章各級投影
- [ ] 數據/易讀:輪色正確、數值直標帶單位、關鍵數字放大、未達成=中性灰、單圖單訊息
- [ ] 頁面模式正確(自適應/16:9/A4);內容超載已拆圖而非縮字
- [ ] 豐富度等級=使用者確認之等級;4/5 級素材皆取自使用者素材庫,整張圖未交由生成模型重繪

## 迭代

使用者的修改回饋若可通用化(新的排版規則、色彩偏好),應建議寫回本 skill 的規格檔並重新打包,讓下一次產圖直接受益。
