# geo-infographic-style 變更清單(貼回原始 repo 用)

## v20260810.01:第十輪(REVIEW 優化)

- **SKILL.md 積極重構**:工作流程 9→6 步;刪除與 style-spec 重複的規格細節(只留節次引用);「強制規則」併入「交付前檢核」,消除雙份維護。
- **版型改為「意圖 → 結構」兩層框架**:先問主要溝通意圖再選版型;新增地理標註、時間軸、Venn 三版型(style-spec §9),補三段敘事骨架與 F/Z 視線動線通則(§11.5–11.6)。
- **去背改為無條件原則**:asset-generation 新增「去背總原則」——所有素材一律去背成透明 PNG、與底色無關;洋紅只是 chroma 手段非判斷條件(`chroma_key.js` 已自動偵測底色)。
- **淺色原則按表面拆分**(style-spec §2 新增「表面分類」):承載文字的表面維持淺色;素材/插圖不受限可深色飽和發光;新增「重點卡」可深底(白字、對比 ≥ 4.5:1、每圖至多 1–2 張)。§5 同步放寬素材色彩。
- **新增「Prompt 交接模式」**(asset-generation):雲端/受限環境無法本機外呼時,Claude 產可貼上的完整 prompt → 使用者自產貼回 → Claude 去背組裝,作為第 3 條正式產圖路徑。決策流程 Q3 加「本機能否外呼」分岔。


本清單彙整九輪優化。前兩輪補規則/修腳本;第三輪圖文結構重構與豐富度 5→3 級;第四輪新增流程版型生成器;第五輪加入場景式素材(圖文呼應)與階梯流程版、放寬素材術語限制;第六輪通用去背 + 素材一律去背浮貼;第七輪清健檢 C1/C3;第八輪清健檢 R1–R3/D1–D3;第九輪清第二次健檢 N1–N6。

---

## 新增檔案

| 檔案 | 用途 |
|---|---|
| `scripts/inject_defs.js` | 自動把 `assets/defs.svg` 的漸層/濾鏡/symbol 注入目標 SVG。冪等、只補缺的 id、不覆蓋圖檔自訂同名 def。用法:`node scripts/inject_defs.js 圖.svg [輸出.svg]`。取代人肉整段複製 defs,避免漏貼/貼舊版。 |

---

## 第一輪:短卡留白規則 + A2 + A3

### `references/style-spec.md`
- **§3.1 新增「短卡留白上限」規則**(強制):同欄任一卡「內容實佔寬 ÷ 卡寬」須 ≥ 55%,低於則須擇一處置——(a) 圖示補位、(b) 該欄放棄強制等寬、(c) 重新斷行填滿。純清單列(list row)不受限。補足原本 governing 卡檢核只驗「最寬卡」、放過短卡自身留白的缺口。

### `SKILL.md`
- 交付前檢核新增「短卡不空」項(對應上述 §3.1 規則)。

### `scripts/render.js`(A2 修正)
- 寬高解析從「全檔第一個 `width="數字"`」改為**只取根 `<svg>` 開標籤**的 width/height,並在缺值時退回 `viewBox` 第 3/4 值。修正命中 defs/symbol/image 內部 width 導致截圖尺寸錯誤、圖被裁的 bug。

### `references/asset-generation.md`(A3 文件同步)
- 產圖章節新增雙腳本對照表:`gen_assets.js`(Node,預設,雙供應商)vs `generate_assets.py`(Python,僅 OpenAI,便捷批次)。原本 py 腳本存在卻未見於任何文件,現已同步。

> 註:第一輪曾列的 A1「defs 缺徽章色」經複查為誤判——`defs.svg` 球面徽章八色齊全(sBlue…sGold),未改動。

---

## 第二輪:R1–R10

### `scripts/render.js`
- **R1**:`waitForTimeout(800)` → `await page.evaluate(() => document.fonts.ready)` + 200ms 緩衝,避免截到字型未載完的走樣圖。
- **R10**:主流程包 `.catch(...) { process.exit(1) }`,失敗回非零結束碼。

### `scripts/chroma_key.js`
- **R2**:新增 `--pad=N`(裁切邊界外擴,預設 8)與 `--alpha=N`(判定屬於物件的 alpha 門檻,預設 20;淺色物件邊緣被裁時降到 8–12)。原本裁切門檻寫死 `al>20`、`pad=8`。
- **R10**:主流程包 `.catch(...) { process.exit(1) }`。

### `scripts/gen_assets.js`
- **R3**:重試時逐次追加升級版「禁止任何文字」語氣(`NOTEXT_ESCALATE` 三段),降低生成模型漏字機率。
- **R9**:新增尾句模板 `TAIL` + `withTail()`——manifest prompt 若省略固定尾句自動補上(已含者不重複);`prompts.json` 留存改存補全後的 `basePrompt`。
- **R10**:累計 `failed` 計數,>0 時 `process.exit(1)`;主流程包 `.catch`。

### `scripts/generate_assets.py`
- **R9**:同步尾句模板 `TAIL` + `with_tail()`,與 Node 版一致。

### `SKILL.md`
- **R4**:交付前檢核改為指向 `style-spec.md §X`,去除與規格的逐條重複。
- **R5**:16 項扁平清單重構為兩層——**硬性紅線 6 條**(內容忠實 / 卡寬留白 / 間距 / 淺色 / 字級 / 零重疊)+ **品質項**。
- **R6**:工作流程步驟 6 加註可用 `inject_defs.js` 自動注入 defs。
- **R7**:版型判斷區新增「混合結構 / 對不上版型」fallback 指引(取主關係為骨幹、次要用疊加語彙、不行就拆圖)。

### `references/style-spec.md`
- **R8**:§11 易讀原則新增第 4 條「不只靠顏色(無障礙)」——紅綠相鄰對色盲不友善,群組/類別另配圖示或標籤、狀態用形狀區分、相鄰序列避免紅接綠。

### `references/asset-generation.md`
- **R2/R3/R9/R10** 對應說明同步:去背腳本用法加上 `--pad`/`--alpha`、重試升級禁文字語氣、尾句自動補全、失敗回非零結束碼。

---

## 驗證狀態
- 全部 JS 腳本 `node --check` 通過;`generate_assets.py` `ast.parse` 通過。
- `inject_defs.js` 已功能測試:注入成功、重跑冪等、不產生重複 id、不覆蓋既有同名 def。

## 未處理(可下一輪評估)
- B1/B2 更深層的 defs「單一來源」自動化(目前 inject_defs.js 已解決主要痛點)。
- 真正的 OCR 文字偵測自動重產(R3 目前為 prompt 升級,非影像偵測)。

---

## 第三輪:圖文結構重構 + 豐富度 5 級→3 級

### 新增檔案
- `scripts/gen_role.js`：依「圖文結構語法」規則生成角色/中心輻射圖的參考實作(3 級同構、圖文並置 35:65、hub-spoke 連線)。內含示範資料,他案替換資料即可重用。
- `scripts/gen_role.README.md`：上述生成器的通用使用範本(環境、用法、三級與 `DECOR`/`SCENE` 開關、替換資料、icon 對應)。

### `references/style-spec.md`
- **新增 §14 圖文並置卡與 hub-spoke 版式**:主圖區 35% + 右清單 65%、左右鏡像、三級圖像尺度、hub-spoke 連線佔中央帶、核心卡維持淺色(結論:不改深色)、場景素材必須落地。
- **§13 改寫為 3 級**(原 5 級):1 線框 / 2 立體(裝飾為級內開關)/ 3 素材場景(場景為級內開關);新增「三級同構原則」。
- **§3.1 收緊清單列豁免**:素材撐大卡、列全半空時不適用豁免,改用 §14。
- §5、§3.1 內所有「4/5 級 / 1–5 級」等級用語同步改為 3 級制。

### `SKILL.md`
- frontmatter 描述、工作流程(步驟 1/2/4/6)、交付前檢核 全部由 5 級改為 3 級。
- 檢核新增:圖文並置(§14)、中心輻射中央帶、三級同構 三項;核心卡改為「維持淺色」。

### `references/asset-generation.md`
- 標題與內文的「4/5 級」全部改為「第 3 級」(素材現集中於第 3 級)。

> 說明:此輪把「裝飾」與「場景」從獨立等級降為級內布林開關(`gen_role.js` 以 `DECOR`/`SCENE` 環境變數控制),5 級能表達的畫面 3 級全可做到,只是不再各佔一個編號。核心 hub 卡經討論**維持原淺青綠配色,不改深色**。

---

## 第四輪:流程版型生成器 + 產圖教訓固化

### 新增檔案
- `scripts/gen_blueprint.js`:流程/roadmap 版型(16:9)參考實作——上升階段卡 + 全寬淺色基礎橫帶 + 第 3 級嵌真素材,對應版型判斷表「流程」列。

### `references/asset-generation.md`
- **材質不透明鐵則**:透明/磨砂/玻璃材質會把洋紅 chroma 背景吃進物件變粉紅;prompt 一律指定 opaque solid(NOT glass/transparent)。
- **金鑰洩漏處置**:金鑰若被貼進對話/指令/日誌即視為洩漏,完成後立即撤銷/輪替。

### `references/style-spec.md`
- **§6 排版鐵則**:英文/數字/代號詞(OIDC、SSO、API、RBAC、UPLOAD_*、PWA)不得跨行切斷。
- **§3.1 固定高卡片垂直留白**:內文短時主圖放大置中填滿剩餘空間,不得中段大片空白。

> 來源:一次完整「重繪流程圖」實作(計畫未來發展藍圖 L3)中,逐步驗證 skill 流程並把踩到的坑固化成規則。

---

## 第五輪:場景式素材(圖文呼應)+ 術語規則放寬 + 階梯流程版

### 新增檔案
- `scripts/gen_scene.js`:多模態場景式素材——風格參考圖 + 內容 prompt → 一整組會呼應文字的等角場景(玻璃風格免洋紅去背)。
- `scripts/gen_blueprint2.js`:流程/roadmap「階梯成長」佈局變體(卡片逐階上升 + 成長曲線 + 淺色基礎橫帶)。

### `references/asset-generation.md`
- 新增「**場景式素材**」路徑:鎖風格(參考圖)、元件呼應內容、免去背(白底/卡色底全滿版嵌入)、換色、術語處理。
- 驗收放寬:允許拼字正確的簡短專案術語。

### `references/style-spec.md` · `SKILL.md`
- **術語規則放寬**:素材可保留簡短專案術語(AI/KPI/GIS/API/SSO)作視覺標記,條件為拼字正確、僅短詞、不承載資訊;承載資訊文字仍由 SVG。(§5、驗收、SKILL 兩處)

> 來源:重繪「計畫未來發展藍圖」時,為達成「圖文呼應」發展出場景式素材流程,並依使用者回饋放寬素材文字限制。

---

## 第六輪:通用去背 + 素材一律去背浮貼

### `scripts/chroma_key.js`
- **通用純色 chroma-key**(不再寫死洋紅):新增 `--bg=#hex` 指定背景色,省略則**自動偵測四角背景色**;以顏色距離 `--tol` 判定 + 羽化 + 裁切。綠幕/白底/任意純色皆可。

### `references/asset-generation.md`
- §4 去背改為「通用純色」章節;立**原則:素材一律去背成透明 PNG**。
- 場景式素材:修正先前「免去背」說法——**玻璃改「不透明霧面」再洋紅去背 → 透明浮貼**(真半透明玻璃走 OpenAI 真透明)。

### `scripts/gen_blueprint.js` · `gen_blueprint2.js`
- 場景改「透明去背浮貼」(`<image meet>`、不裁切、不墊底色,卡片漸層透出)。

> 來源:使用者指正「場景式素材仍應去背、且去背不限洋紅」。連帶解掉健檢 C2(玻璃材質):改「不透明霧面」後既能去背又保有玻璃外觀。

---

## 第七輪:健檢 C1 + C3 收斂

### C1 · `scripts/gen_assets.js` + `generate_assets.py`
- 尾句拆「核心 + 禁文字」兩段;新增 **`ALLOW_TERMS`** 環境變數:設定時自動移除 `no text` 限制並放行短術語(如 `ALLOW_TERMS=1` 或 `ALLOW_TERMS=AI,KPI`),與 §5 術語放寬對齊。預設行為不變(仍禁文字 + 重試升級)。
- `asset-generation.md §5` 驗收改指向 `ALLOW_TERMS`(取代「手動移除尾句」)。

### C3 · `references/style-spec.md §3.1`
- 把四條重疊留白規則(清單列豁免 / 豁免收緊 / 短卡 55% / 固定高垂直)**收斂成一棵決策樹**:①卡寬由內容決定→過 ②內容填充率≥55%(含垂直)→過 ③否→§14 圖文並置/放大主圖/縮欄/重斷行。清單列豁免僅在「至少一列夠長」時成立。

> 來源:健檢報告 C1(工具與術語規則漂移)、C3(留白規則過度分裂)。R1–R3、D1–D3 待後續。

---

## 第八輪:健檢 R1–R3 + D1–D3(冗餘收斂 + 可發現性)

### 冗餘收斂
- **R1 插圖指引整併**:§5 標明為插圖/材質「唯一出處」;§13 L2 移除重述、改指回 §5 + `richIllo()`。
- **R2 場景/裝飾整併**:§12 標明為裝飾/場景「唯一出處」;§13/§14 只引用。
- **R3**:§3.1 留白已於第七輪 C3 收斂成決策樹(此輪不再重複)。

### 可發現性
- **D1 生成器進主流程**:SKILL 版型判斷區加「參考生成器」對照(角色→gen_role、流程→gen_blueprint(2)、素材→gen_assets/gen_scene);step 6 加「有生成器優先改資料重用」。
- **D2 同構措辭去 hub 綁定**:§13 三級同構改「骨架元素依版型而定」(角色含 hub、流程含軸線+基礎帶、環形含核心圓)。
- **D3 素材路徑決策表**:asset-generation 開頭新增「單物件去背 / 場景式 / OpenAI 真透明」三路徑選擇表。

> 至此健檢報告 C1–C3、R1–R3、D1–D3 全部處理完畢。

---

## 第九輪:第二次健檢 N1–N6

- **N1 工作流程步序修正**:「素材取得」從 step 2 移到 **step 5(拆卡內容之後)**——先版型→規格→先文後框→再取素材(此時每卡素材需求才明確)。
- **N2 asset_prompts 殘留玻璃**:9 筆 `frosted-glass`/`glass` 改為 opaque(與 CHROMA「fully opaque」一致);刻意透明的 P12 光錐/P20 玻璃罐保留並註記「走 OpenAI 真透明」。
- **N3 專案資料耦合**:`gen_role.README.md` 增表,標明 3 支生成器皆為「範例(含示範資料)」與各自替換點;附徹底解耦(抽 examples JSON)之建議。
- **N4 py 冗餘**:`generate_assets.py` 標為 **deprecated**(檔頭 + asset-generation 表),導向 `gen_assets.js`。
- **N5 3 級適用範圍**:§13 註明 3 級主要規範卡片/角色類版型;純數據圖/流程圖以 §7/§9 為主。
- **N6 檢核抽測**:硬性紅線每張必掃、品質項依版型抽測。

> 至此兩次健檢(C/R/D + N1–N6)全部處理完畢。
