# bpmn-flow-builder|skill 版本記錄

| skill 版號 | 日期 | 變更摘要 |
|-----------|------|----------|
| 20260818.02 | 2026-08-18 | 【新增獨立檢核(乾淨 session)+泳道排序慣例】(使用者裁決)①workflow 新增「獨立檢核」節:emit+validate 通過後、交付前,以 subagent(乾淨上下文、無產製過程記憶)檢核三件事——忠實度(圖面 vs 原始輸入)、規則遵循(鐵則+慣例)、**殘餘缺陷挑戰**(對每項殘餘用離線實驗驗證是否真不可避免,不接受未經實驗的「極限」宣稱;同日兩次實證:交叉×2 與繞行×2 均被標極限、實際皆可修)。評審只判定與開單不動手;FAIL 回修再送;兩輪未修好升級使用者;明示快速出圖可跳過須註記。SKILL 執行內容與首次繪圖第 4 步同步掛鉤。②conventions 新增「泳道排序(分流樞紐置中)」節:閘道出邊通往多條泳道的角色道應置目標道之間,引擎不會自動重排,建模時排對;離線互換 lane 比分即可驗證(實證 26→6)。無程式變更。 |
| 20260818.01 | 2026-08-18 | 【側通道就近化收緊:走廊被擋即收緊,不再退到泳道邊緣】(使用者標紅「連線繞太遠」)`_assign_side_channels` 原僅在「節點欄外緣→泳道邊緣的通道地帶」有障礙時才收緊,障礙與欄同寬、右緣切齊的常見情形(旁路繞過單一 task)地帶淨空 → 通道退到 2 倍寬泳道最外緣,實測繞行 1487px vs 直達 293px、route 提示無效。修法:新增 `corridor_blocked` 判定(障礙物與來源↔目標欄位 x 範圍重疊即為需繞),`blk or corridor_blocked` 時通道取 `max(障礙外緣, 欄外緣)+MARGIN`(clamp 泳道邊緣;沿用 20260810 審查修正,sideLeft 端仍以 max 保讓位軌)。實證:會議skill使用情境圖 V01.01→V01.02,繞行過長×2 → 評分 0。回歸 25/25 通過。conventions「通道就近化」同步改寫;SKILL 版號標記與 plugin.json 同步。 |
| 20260817.01 | 2026-08-17 | 【新增「轉折極簡與訊息流平行」慣例】(使用者裁決,文件)conventions 新增一節三條:①跨 pool 訊息流兩端點空間足夠時拉齊同 row 走水平直線、不降一階,拉齊後同步壓縮空列(舊「階梯式列位、訊息流一律向下」模式列為反例);②閘道主線直下 0 轉折優先(主欄淨空不加 sideLeft/sideRight),旁置子欄分支合流自目標右側 enterRight 接入;③明示 route 為「轉折過多/繞行過長」檢核盲區,迭代時應逐條檢視既有 route 提示必要性。實證:法務部不起訴書 V12.04/.05(訊息流拉平+壓縮 3 列)、數位卷證 V09.01/.02(三訊息流拉平、移除沿用 sideLeft)、統計標籤判斷 V17.01(引用→結束改 enterRight)。同步 SKILL 版號標記與 plugin.json。無程式變更。 |
| 20260812.01 | 2026-08-12 | 【修正多 pool `.drawio` 定位重疊 bug】(使用者實測「不管流程怎麼畫兩個 pool 都疊在一起」)Collab 全手動列(need_auto 空)走 `_ensure` 快路徑時,pool 原點在**泳道加寬(工件/最小寬)前**就排定,`_ensure(proc)` 之後泳道加寬使前一 pool 變寬卻未重排原點,後一個 pool 左移、容器框疊進前一個(實例:pool2 標題帶 x 少算 140px)。SVG 因二次 `_ensure` 以加寬後寬度重算而正確、`.drawio` 沿用首次錯值 → 兩格式不一致、`validate_bpmn` 以 SVG 為準攔不到。修法:快路徑於各 pool `_ensure` 後、加寬定案時**重排 pool 原點**(`ox += pool_width()`),原點有變者重放置其節點(含 attach),兩格式一致且不重疊。自動佈局路徑(`_reflow_pools`)本就在加寬後排原點、不受影響。 |
| 20260811.03 | 2026-08-11 | 【SKILL.md 補「支援元素(圖例)」小節】(文件)在 SKILL.md 新增支援元素速覽:列出全部事件型別(含新增 signal/link/cancel/multiple/parallelMultiple)、閘道/活動 kind、連接與工件,並註記 multiple/parallelMultiple 的 .bpmn v1 限制、指向 conventions「圖例落地」看完整樣式對照。同步 SKILL 版號標記與 plugin.json。無程式變更。 |
| 20260811.02 | 2026-08-11 | 【補齊 BPMN 事件類型:signal/link/cancel/multiple/parallelMultiple】(使用者依 Visual Paradigm 事件矩陣圖要求補完)原僅支援 message/timer/error/escalation/conditional/compensation,今補 5 種,位置維度(start/中間 catch/throw/boundary interrupting/non-interrupting/end)沿用既有自動判定,三輸出落地:**signal/link/cancel** 加入 `EVDEF`(signal/link/cancelEventDefinition),自動獲得 .bpmn eventDefinition+位置 tag+邊界+bpmn_import 反解;SVG 新增三角/箭頭/X 圖示;.drawio `symbol=signal/link/cancel`;drawio_import 反解。**multiple/parallelMultiple** 因 BPMN 無單一 eventDefinition(需 ≥2 個)另立 `MULTI_TS` 特case:SVG 畫五邊形/十字、.drawio `symbol=multiple/parallelMultiple` 完整,.bpmn 輸出無定義事件(位置決定 tag、parallelMultiple 標 `parallelMultiple="true"`,不偽造 multipleEventDefinition)——bpmn.io 需 ≥2 定義才繪五邊形,故 .bpmn 顯示素圈(v1 限制,已註記)。配套:新增 `TRIGGER_TS`(= EVDEF+MULTI_TS)統一「無 incoming 觸發起點/中間 catch」語意豁免(原僅 message/timer,signal 起始不再誤判孤兒)、STYLE 補 5 色盤、SVG 雙圈判定與 add() kind 驗證改用 TRIGGER_TS、TYPE_ZH 補中文名。tests 新增 4 項(共 25,全通過):三輸出良構+鐵則、eventDefinition 齊備且 multiple 不偽造、drawio symbol、signal 起始非孤兒。conventions 圖例表補兩列+ .bpmn 限制註記。 |
| 20260811.01 | 2026-08-11 | 【排他閘道改畫 X 記號(回歸 BPMN 原始規範)】(使用者裁決:依最原本規則補 X)原設計為「素菱形+名稱置菱形內」,今改為菱形內畫 `✕`、名稱移至菱形下方,三輸出一致:①SVG else 分支改畫 X(對角雙線 stroke-width 2.6),名稱走 _svg_below_label(與 parallel/inclusive 同)②`.drawio` `_DIO_GW_KIND["exclusive"]` 由 `symbol=none` 改 `symbol=exclusiveGw`(名稱本就 verticalLabelPosition=bottom;drawio_import 仍以 fallback 判為 exclusive,往返不變)③`.bpmn` BPMNShape 補 `isMarkerVisible="true"`,bpmn.io 才於菱形內繪 X。名稱移出菱形後移除「排他閘道名稱 ≥5 字溢出菱形」check_layout 檢查與 conventions ≤4 字硬限;下方標籤碰撞檢查(壓線/壓節點/超 pool 邊緣)擴及 gateway(原僅 EVENT/ARTIFACT),名稱過長改由通用標籤檢核把關。另依 dotblogs BPMN 規範文比對:conventions 圖例表標注 input/output=**Data Object**、database(圓柱)=**Data Store**(兩者分屬 BPMN 兩類工件)。tests 21/21 全通過;conventions/內嵌 docstring 同步。 |
| 20260810.06 | 2026-08-10 | 【新增橫式版面 Batch 2】(使用者需求)Proc 新增 horizontal=True:流程左→右、泳道成上下橫帶。作法與已調校的直式引擎**隔離**——重用直式引擎算好的 row/sub/lane 邏輯結構(方向無關),佈局後由 _apply_horizontal 改以橫式座標放置(row→x 間距 H_COL、sub→y、泳道→上下帶)並以 _route_h 橫式正交路由重算 waypoints(前向直線;會穿中間節點者走上方繞行通道;閘道多出邊分埠;回頭邊上方繞回)。三輸出全支援:SVG 新增 _svg_pool_h(橫帶+左側標頭)、drawio 橫式 pool/lane cell、.bpmn DI isHorizontal=true;build_svg viewBox 改依節點/橫帶範圍;壓泳道線/標籤超界檢核對橫式/簡易豁免。可與 simple 併用(橫式+無框)。example 增 build_horizontal/build_horizontal_simple(納入不變量測試),tests 增 2 項(共 21,全通過);SKILL/API 文件同步。註:橫式 v1 的 bands(系統分區)暫不繪、複雜分支路由未如直式引擎精修,線性/一般分支流程已乾淨(實測 0 違規)。 |
| 20260810.05 | 2026-08-10 | 【新增簡易模式:流程圖可不套 BPMN 泳道】(使用者需求)Proc 新增 simple=True:lanes 可省略(內部單一隱藏泳道)、add() 的 lane 預設 0;三輸出在 simple 時**不畫 pool 外框/泳道分隔/分區/標頭**,只畫節點+連線(SVG 仍保留圖頂標題與 <title>;viewBox 改依節點範圍修掉表頭留白;.bpmn 省略 laneSet 與泳道 DI)。適合單純流程、無角色分區時用;直式+泳道仍為預設。example_process 新增 build_simple()(納入不變量測試);tests 增 4 項(共 19,全通過);SKILL/模組 API 文件同步。橫式版面為下一批(Batch 2)。 |
| 20260810.04 | 2026-08-10 | 【檢視器無障礙/行動裝置 + SVG aria】(第三輪待辦專項之一,與佈局引擎隔離)①單頁與多頁檢視器改用 Pointer Events(滑鼠+觸控+筆)+ 雙指 pinch 縮放——手機/平板可平移縮放(原僅綁 mouse/wheel,行動裝置完全無法操作)②stage 加 tabindex/role=application/aria-label 與鍵盤操作(+/− 縮放、方向鍵平移、0 重置)③搜尋框加 aria-label、命中數 aria-live=polite 播報、focus-visible 樣式、提示文字對比提高 ④build_svg 根加 role=img/aria-label/<title> 供螢幕閱讀器。tests 新增 test_svg_has_aria_and_title、test_viewer_touch_keyboard_aria(共 15 項,全通過);回歸專案圖鐵則全通過。|
| 20260810.03 | 2026-08-10 | 【新增自動化測試套件】(補「零自動化測試」體質缺口)新增 `tests/test_bpmn_builder.py`,13 項、可 `python3` 直跑亦相容 pytest:①單元 _ncname(保留字改名/中文轉 ASCII/合法保留)、bump_version(結構/文字進位、次版無上限、非法 raise)、_clean(濾控制字元保留 \t\n)、_safe_stem(路徑穿越防護)、_boxes_overlap ②不變量(最高 CP):reference 五範例(auto/bands/collab/manual/parallel)emit 後鐵則硬傷=0 ③良構:五範例 build_bpmn/drawio/svg 皆合法 XML ④安全:惡意名稱(引號/控制字元/&/<)三輸出仍良構、控制字元被濾 ⑤往返:emit→drawio_import/bpmn_import 節點集合一致。把 CHANGELOG 長年人工「回歸 5 範例」變成可重跑守護;workflow「效率規則」新增「改動引擎/腳本後必跑 tests,FAIL 即回歸勿交付」。13/13 通過 |
| 20260810.02 | 2026-08-10 | 【第三輪新角度審查:8 項快贏】(安全/效能/測試/BPMN 符合度 4 新角度平行審查)。**安全**:①.bpmn 屬性改 quoteattr(_attr + lane/process/participant/node/dataObject/dataStore/boundaryEvent/categoryValue 各 name=/value=;escape 不跳脫引號→名稱含 " 會屬性注入/非良構)②新增 _clean 過濾 XML 非法控制字元(保留 \t\n\r),於 Proc/add/flow 入口套用 ③新增 _safe_stem 清洗檔名(去目錄成分+非法字元),emit/emit_multi 的 stem 與 project 套用,防路徑穿越。**效能**:④auto_layout 候選達 0 缺陷即早停(省 5-8 候選);⑤加寬逐級試算加 ENGINE_TIME_CAP 時限保底(超時採當前 best,大圖不再失控)。**健壯性**:⑥check_semantics 補「順序流引用不存在節點」警示(原幽靈邊靜默丟棄);⑦add() 重複 id 警示、bpmn_import 缺 x/y 給預設。**輸出**:⑧MD TYPE_ZH 補全 terminate/message/timer/工件等(消中英混雜)。實測惡意名稱(引號/控制字元/&/<)於 bpmn/drawio/svg 三輸出皆良構;回歸專案 5 組 10 頁鐵則全通過、無回歸。第三輪其餘中大型項(檢視器行動/鍵盤/aria 無障礙、terminate 三輸出一致、事件角色 classify_event 抽共用、pairwise 空間索引、waypoints memoize、零自動化測試補測試套件)列入待辦專項 |
| 20260810.01 | 2026-08-10 | 【四角度全面 REVIEW 修正】(平行代理審查 59 項發現,依高→中→低修)。**正確性 3**:①lane0 多回線巢狀方向反了(pool_left+blt*gap 把大跨度塞內圈致交叉)→改 (maxblt−blt) 維持大跨度在外;②回線配軌移到選路定案「之後」再算,只計最終 rt=="backLoop" 者(原在選路前把改走側通道者也配軌,留有洞巢狀序);③_near_cx 覆寫 sideLeft 改取 max(near, 讓位軌基準),保留 _bndL 讓位量。**還原 2**:bpmn_import 事件/kind 反向對照(重用 EVDEF,原除 gw/start/end 全塌成 task);drawio_import 端點脫鉤連線改收集並於 show/diff 警示(原靜默丟棄)。**API/打包**:Proc 建構子 bands/version/ox 改關鍵字-only(杜絕 band 誤塞 ox);add() 驗證 lane 範圍並提示「系統分區請用 bands=」;flow() 驗證 route(VALID_ROUTES);emit 複製定義檔改可攜 sys.path 定位(環境變數→marketplace 探測→原路徑 fallback);bands 成員 id 拼錯警示;build_drawio_multi 下沉跨頁 id 唯一性檢查;.bpmn collaboration name 併入版號;bpmn_import 補 sys.path 注入。**輔助去重**:validate_bpmn 改 import bpmn_builder._JS_RESERVED(fallback 保 standalone)。**文件**:internals 補 20260809 就近繞線四機制+本輪三修、改回線分軌排序敘述;conventions 規則 4/5 更新;example_process 檔頭改「.drawio 預設/必產3+可選3」;plugin.json、validate docstring 去「6 檔」漂移;CHANGELOG 歸檔至最近 10 筆。**清理**:FRAME_TOL 錯位註解、函式內重複 import re、bpmn_import 死變數 node_pool。回歸專案 5 組 10 頁鐵則全通過、無回歸。**暫緩(高風險純重構,已確認為真、留待專項)**:線色/訊息色集中 STYLE、_auto_routes 8 階段拆解與常數集中、幾何函式抽 geom.py、force_split 與端口消解合併、database 型別統一、工件彈性拓寬(art_subs)、_xy_manual 手動絕對座標保護 |
| 20260809.04 | 2026-08-09 | 【回線通道就近化＋巢狀不交叉三修】(使用者實證:lane1+ 的回線繞到 pool 最左框緣、雙回線交錯)①waypoints backLoop 通道由一律貼 pool 內緣改為「來源泳道左緣外側的相鄰空隙」(lane1+ 落在與左鄰泳道之間、近節點;lane0 無左鄰則沿用 pool 內緣),多回線逐軌外移 ②_assign_backloop_tracks 軌位改依「列跨度(hi-lo)升冪」指派——跨度大的回線佔外軌才能同心包住內圈,涵蓋並取代原「同目標深源佔外軌」規則,額外正確處理不同目標的巢狀回線 ③_spread_ports 垂直側接點排序新增方向翻轉:原「外軌配上位接點」僅適用垂直段在接點下方(節點為目標、由下往上進)的情形;垂直段在上方(節點為來源、線往上離開,或同節點混有進/出線)時改「外軌配下位接點」,以對向端 y 判斷。實測統計標籤判斷_執行類「上訴駁回→是」與「重查」由貼框緣、雙交錯 → 泳道間空隙同心巢狀、零交錯(評分 10→0);回歸不起訴書/數位卷證/約談現行全數鐵則通過無新增違規 |
| 20260809.03 | 2026-08-09 | 【側通道清距加大】(承 .02 使用者回饋:貼障礙外緣僅 16px 太近,帶標籤或字多會壓到節點)_assign_side_channels 的 MARGIN 由固定 LINE_GAP+4(16)改為 max(36, LINE_GAP+4+(24 if 有標籤 else 0)):無標籤留 36px 舒適下限、帶標籤再加 24px 供文字留白;仍 clamp 不超過泳道邊緣。實測「引用→否」通道由 x=1415 移至 x=1439(離 task 右緣 40px),鐵則續全通過 |
| 20260809.02 | 2026-08-09 | 【側通道就近化】(承 .01 同一類回饋:向下分支/跳段線為繞過中間節點而走 sideRight/sideLeft,通道 x 寫死泳道邊緣→繞太遠、標籤貼分區線)新增 _assign_side_channels(p)(於 _auto_routes 開頭、_assign_backloop_tracks 後呼叫):對每條邊預算左右近通道 x=「擋在來源↔目標間、y 範圍重疊之節點外緣 + (LINE_GAP+4)」,clamp 不超過泳道邊緣;**僅偵測到障礙需繞時才寫入**來源節點 _near_cx[(route,目標id)](無障礙維持泳道邊緣,不動既有無側繞版面)。waypoints() 的 sideRight/sideLeft 改優先讀 _near_cx。實測統計標籤判斷_執行類「引用→否」由 x=1536(泳道右緣)改走 x=1415(貼 task 右緣),標籤同步內移離開分區線;回歸專案內 5 組流程圖(不起訴書/數位卷證/約談現行/統計偵查+執行)鐵則全數通過、無新增違規 |
| 20260809.01 | 2026-08-09 | 【回頭邊就近繞線】(依使用者實證回饋:單一迴圈、右側有空檔時仍繞到 pool 最左緣大通道,捨近求遠)_auto_routes 的路由決策原將所有 T.row<S.row 的回頭邊無條件設 r="backLoop"(pool 左緣專用大通道);改為候選試算:對 ("sideLeft","sideRight","backLoop") 各算 waypoints 後以 (conflicts, 路徑長) 取最小——乾淨時泳道邊緣近通道路徑較短必勝、只有與節點或其他既定線重疊時繞遠的 backLoop(衝突較少)才勝出,即「就近為主、有重疊才往左右繞」。多回線仍可退回 backLoop 各佔一軌(_assign_backloop_tracks 不變);二輪精修對非 backLoop 邊照常重選候選。實測統計標籤判斷_執行類「重查」迴圈線由 x=184(pool 左緣)改走 x=644(AI 泳道左緣近通道),雙頁鐵則仍全數通過 |

> 較舊條目(64 筆)已歸檔至 `CHANGELOG_archive.md`。
