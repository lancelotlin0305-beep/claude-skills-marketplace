# bpmn-flow-builder|skill 版本記錄(歸檔)

> 主檔 `CHANGELOG.md` 保留最近 10 筆;本檔存放其餘歷史條目。

| skill 版號 | 日期 | 變更摘要 |
|-----------|------|----------|
| 20260716.07 | 2026-07-16 | 【錨定連法改透明錨點+手動工件座標尊重修復】(使用者截圖實證×2)①draw.io 邊對邊連接不可控——實測其端點無視顯式 points、自行重佈成斜線黏到附近節點;改於錨定點放 2×2 透明錨點 cell,虛線連錨點(exit/entry 釘 0.5),端點固定不可重佈 ②「工件明給 row 者尊重」承諾在自動佈局下實際失效:_try 把 depth 重派給全部節點(含工件)、候選/優勝套用兩處都會蓋掉手動座標——新增 p._art_manual 快照(首次 auto_layout 時取),_try 內還原+過濾複本(勿就地 pop:_layer 字典可能快取重用)、優勝套用迴圈跳過手動工件 ③實戰:註解手動置於否線底段旁(結束事件下、與否線同側),虛線垂直短接,兩頁交叉歸零評分 0(此前註解在右、否線在左,任何路徑必穿中央鏈或是線;引擎對 side 提示會左右試算擇優,使用者的 sideRight 提示會被翻面——同側訴求以工件手動座標實現,非 route 提示) |
| 20260716.06 | 2026-07-16 | 【水平跳段避開分區線】(使用者實見:lane 交接跳段沿「波特玩系統」分區線行走,FRAME_TOL 內鐵則違規;且指出即使勉強分得開、距離 <2×LINE_GAP 人眼仍難辨)根因:_orth_down 的跳段 cy=ty-GAP 與 band_spans 邊界同以列格線推得,band 起始列即 lane 交接列時必然共線。新增 _auto_routes 末端後處理 _dodge_band_lines:①水平段(>30px)距任一分區線 <2×LINE_GAP 時,於上下鄰折點間搜尋離所有分區線 ≥24px 的淨空 y 平移(±24/±36/±48 候選,須不穿節點、不與他邊重合/貼行)②走廊過窄無淨空位(列間隙 <96px 常態)→ 改側進:垂直下行至目標中心高度、水平進目標側端口,水平段落在分區內部;找不到仍保留原樣由「框線重疊」如實回報。結果持久化 wps_override(三輸出與檢核共用)。實測數位卷證標籤製作圖 5→0 分;迴歸統計標籤判斷圖前後同分(3,繞行提醒不變) |
| 20260716.05 | 2026-07-16 | 【註解疊線與錨定線穿節點三修——結構性根治】(使用者實見 .04 兩問題仍在;教訓:shape label 內距在不同 draw.io 版本括號深度不一,調參數治標)①.drawio 註解拆三 cell:透明本體框(關連端點釘選、幾何檢核基準)+12px 括號 cell(__br)+18px 起獨立文字 cell(__tx)——座標分離為結構保證,不再依賴 spacingLeft ②錨定關連的錨點顯式寫入邊的 points 陣列(重複末點):target=邊 cell 時 draw.io 會自行重佈末段而繞穿節點,顯式路徑點後照走 ③validate_bpmn 重疊檢查豁免複合元件子件(id 同「本體__」前綴) |
| 20260716.04 | 2026-07-16 | 【錨定線避讓+括號內距二修】(使用者實見:錨定虛線橫穿「檢視判讀資料」節點;文字仍疊括號線)①_assoc_route_flowref 重寫為候選評分:錨點候選=流程每線段 25/50/75% 點(過短線段跳過)、節點四邊中點皆候選、進線末段垂直於流程線段(沿線進入會與順序流重合),評分=穿節點×20+轉折+長度/1000 取優 ②錨定線納入 check_layout 邊清單(原 guard 只認節點端點→穿越漏檢)③.drawio 註解 spacingLeft 16→24(annotation_2 括號深度大於 SVG 的 12px)、note 寬 150→160 保 11 字/行 |
| 20260716.03 | 2026-07-16 | 【關連錨定到順序流+註解文字疊線修復】(使用者實案:註解應說明「符合→否」分支線而非閘道;文字疊在括號線上)①assoc 端點支援 tuple ("流程來源","流程目標") 錨定到該順序流最長段中點——_flow_anchor 取錨點、_assoc_route_flowref 正交走線、放置夥伴取流程來源節點;.drawio 輸出原生線連線(target=邊 cell id,emit_edge 非節點端點跳過 port 釘選)、.bpmn association ref 該 sequenceFlow id、SVG 畫至錨點 ②SVG 註解文字起點 x+8→x+16,移出開口括號(0~12px)不再疊線;conventions 圖例表新增「關連錨定到線」列 |
| 20260716.02 | 2026-07-16 | 【註解溢出泳道修復】(使用者 draw.io 實見:註解文字一半在泳道外).drawio 註解樣式原用 labelPosition=right 把文字掛在幾何框右側外部——框外標籤不算幾何、逃過鐵則②/壓線檢核卻實際溢出泳道。修二處:①註解標籤改收框內(align=left+spacingLeft=16+whiteSpace=wrap,移除 labelPosition=right)②node_size 的 note 高度隨字數自動加高(11 字/行、14px 行高,下限 44),SVG/drawio/檢核三方同一幾何。實測 28 字註解 150×58 完整收在泳道內 |
| 20260716.01 | 2026-07-16 | 【版號子目錄存檔】(依使用者指示:各版產出勿混在同一資料夾)①emit/emit_multi——5 個帶版號交付檔改存 `outdir/{版號}/`(自動建目錄;outdir 已是該版號目錄時不重複巢套),跨版累積的 `_版本記錄.md` 仍留 outdir 上層 ②validate_bpmn——資料夾掃描改遞迴(`**`)涵蓋版號子目錄;交付完整性的版本記錄表查找相容「同層或上一層」③workflow.md 交付物節載明新目錄佈局。另修 bpmn_builder `_place_artifacts` 泳道全滿備援分支 NameError(`li` 未定義→`n["lane"]`,單泳道掛 3 工件實案觸發) |
| 20260710.16 | 2026-07-10 | 【使用者三輪標紅:雙頁鐵則歸零達成】三輪紅線 6 條(A-1 2 條為已橋接結構性交叉;A-2 4 條為真違規)。修四處:①閘道端口衝突消解(_auto_routes 第二輪半之二)——菱形豁免端口分散,進線與出線共用頂點時把可改路的邊改走未佔用頂點的最佳候選(backLoop/bndSide 不動由他邊讓位);實測 straight 頂點進入本就乾淨卻被選走 sideLeft 共用左頂點 ②分邊↔端口消解跑兩輪收斂——消解改路會在上游閘道生成新共點(議約?實證),分邊邏輯抽為 _force_split_gateway_pairs 於消解前後各跑一次 ③關連線走廊避讓 _assoc_dodge——與先決關連共線/長並行或穿節點時,中段水平/垂直段平移 ±12/±24 取首個乾淨位;④首段插折末手+二階組合——端點段不能整段平移(起點釘在夥伴上),夥伴正下方貼節點時起點後 18px 先橫移再續行,並與走廊平移組合(插折解垂直段、平移解水平段;決標通知×未得標通知對實證單一手段皆不足)。成效:A-1 評分 26、A-2 評分 22,雙頁鐵則全數通過(殘餘僅已橋接交叉與工件貼鄰提醒);圖面依版號規則進版 V01.02;回歸 5 範例+請假全 0 分 |
| 20260710.15 | 2026-07-10 | 【使用者二輪標紅:鐵則歸零攻堅,A-1 達成】診斷發現使用者標紅檔為 .13 世代舊產出(draw.io 舊工作階段存檔蓋掉重產檔),據此比對現行幾何後修五處:①enterRight 方向防呆 _er_ok——目標在來源右側時其末段須橫穿目標本體接右緣(對向端),而衝突分豁免自身端點完全看不見;首輪候選/避讓 fallback/二輪精修三處全面把關,fallback 改側通道試算 ②新增「穿過自身端點節點」檢核——端點前後 12px 落在自身節點內部即報(堵住①的檢核盲區)③回線「深源佔外軌」——同目標多回線同心巢狀的充要條件是最深來源佔最外軌+最上進點;_assign_backloop_tracks 改 (T.row,-S.row) 排序,_spread_ports 垂直側 _away 改按鄰接點 x(左側升冪/右側降冪)排序(原按 y 在初始同值時退化為按 fid,外軌常配到下位進點而交叉)④同源閘道雙出邊強制分邊後處理(鐵則優先於評分):共線或共出點的閘道雙出邊試三種組合(雙側出/單邊側出×2),驗證互不重合、出點相異、不反向繞穿菱形、不穿其他節點才採納;曾試把 outLeft/outRight 加入二輪精修候選,全域波及使範例_平行作業平白多交叉,撤回改針對性後處理 ⑤工件放置避開閘道列(gwpen 次序鍵)——閘道列是分支側出走廊,工件落此堵死分邊(決標記錄表/廢標結果通知擋住達底價?實證)。成效:A-1 鐵則全數通過(評分 26,殘餘僅已橋接交叉×2+繞行/轉折提醒);A-2 剩議約/決標區 5 項(閘道進出共點、雙關連長並行,列後續:閘道進出端口分配、關連線繞行候選);回歸 5 範例+請假全 0 分。使用者標註檔備份於 nsrrc/標註備份/ |
| 20260710.14 | 2026-07-10 | 【使用者標紅回饋:重疊/交錯防治全面補強】使用者於 V01.01 標紅 18 條重疊/交錯連線,幾何診斷揭露檢核器重大漏報:_check_placed 重合/間距檢核對「共端點邊對」整組豁免→同目標雙回線 720px 完全共線、同源閘道雙出邊 230px 共線、同夥伴雙關連線全程同路,全數漏檢且引擎不修。修七處:①移除共端點豁免(端樁由 trim 排除) ②長並行雙門檻——_wps_par_close 增 minlen 參數,>150px 並行須 ≥24px(12px 長並行人眼實證仍視為重疊);共端點對僅檢長並行 ③backLoop 逐軌:_assign_backloop_tracks 寫入 _blt、左通道逐軌外移 2×LINE_GAP,消除多回線共線 ④_spread_ports 鄰接段 >150px 時錯開升 2×LINE_GAP ⑤關連線全體互覺:_route_assocs 一次決定全部(重合×8/端口×2/貼行×5 互斥分+讓位遞移),快取隨 _auto_routes 失效——原各條獨立選路彼此不可見 ⑥conflicts 重合權重 1→2(8 過重會把貪婪決策推出新交叉,範例_平行作業實證) ⑦loopsrc 最左:迴圈親和分欄內回頭邊起點排最左(含全迴圈列),修 A-2 流標回線橫穿開標之退化。成效:紅線重疊/長並行 15+對→3 對;A-1 剩閘道雙出邊重合×1+長並行×2、A-2 剩決標區跨列共欄重合×3+關連穿菱形×1(已知極限,列後續:水平分軌/欄位保留);回歸 5 範例+請假全 0 分。教訓:檢核豁免=引擎盲區,寧可如實計分再由權重取捨 |
| 20260710.13 | 2026-07-10 | 【向使用者手修學習:迴圈親和分欄】使用者於 draw.io 手修 NSRRC A-1(drawio_import diff 判讀:決標鏈 win/notify/awnt 整條 sub0→sub2、廢標鏈 fail/failnt/failnd sub2→sub0/1;所謂「刪除連線」實為拖動時端點脫鉤、Text 為誤加空標籤,均非本意)。泛化為佈局規則:①auto_layout 新增 loopish 標記(DFS 拓撲序下能沿前向邊走到回頭邊起點的節點)②新增 chainL/wideL 候選——迴圈鏈×終止鏈混列時固定分欄:迴圈鏈靠左(鏈內 pos 反序,越近迴圈出口越靠左)、終止鏈靠右,各鏈直下不蛇行;純同類列維持蛇行;成員數>lane_k 退回原邏輯 ③候選加入單 Proc 與 Collab 兩處逐級加寬迴圈,評分取優不強制。實測 A-1 63→40 分、穿節點/連線重合歸零(與使用者手修方向一致且更乾淨);A-2 62 分不變(其重合屬跨列共欄,列後續方向);回歸 5 範例+請假全 0 分。NSRRC 進版 V01.01(change_source=使用者修改 .drawio,使用者手修之 V01.00 檔案保留不覆蓋) |
| 20260710.12 | 2026-07-10 | 【九條鐵則落地+首發品質迭代】(使用者五問裁決:鐵則違規交付但顯著標示/交叉殘餘加跨線橋/全泳道一律2倍寬/引擎時限5分鐘每張/簡單案直接產出)。程式五處:①_lane_min_width——全泳道≥2×LANE_W、單泳道格點節點>20個≥3×,lane_pad對稱補足置中(鐵則⑦);候選/定案/手動路徑皆套用;Collab新增_reflow_pools於佈局後重算pool原點+重放置+自動池重選路(修既有pool原點先於佈局計算的隱患),候選state擴存lane_pad/wps_override/ox ②_spread_ports(鐵則⑥)——選路定案後同矩形節點同側共用端點的邊沿側錯開≤LINE_GAP,端點+鄰接折點同步平移保正交,寫入wps_override;圓/菱形豁免 ③跨線橋(鐵則⑤)——.drawio加jumpStyle=arc;jumpSize=10(_DIO_EDGE/_DIO_ASSOC),SVG新增_hops_path於嚴格交叉處畫半圓弧、_svg_seqflows/_svg_assocs共用segs登錄後畫者跳線;交叉自此不列違規、列提醒(emit以SVG實際橋數如實回報) ④新增檢核「節點壓泳道線」(鐵則②,權重5,邊界事件豁免) ⑤emit/emit_multi回報改版——IRON_KEYS鐵則分類(✗前綴)、_iron_md_section寫入.md「鐵則檢核」區、每張圖引擎計時+ENGINE_TIME_CAP=300超時警示;另修_flow_port_cache未隨重選路失效的陳舊快取。文件:conventions三條鐵則→九條鐵則+檢核表新列、workflow確認先行→確認分級(簡單案直接產出)、internals新增鐵則機制節。實測:泳道加寬使衝突大減,NSRRC引擎42s→28s(A-1 4.9s+A-2 23.3s),A-1端口重合5→2、A-2端口重合3→0;範例/請假全綠 |
| 20260710.11 | 2026-07-10 | 【引擎確定性修復+熱點微優化】(依 NSRRC 採購案實戰計時歸因,程式 2 類+文件 1 處):①【確定性】_repair_rounds 候選迭代兩處 set(涉事節點/涉事泳道)改 sorted()——擇優嚴格 < 平手取先者,候選順序隨 PYTHONHASHSEED 變動使同一定義隨機落在不同版面(實測 A-2 在 55/70 分兩解間跳動);修後 3 seed 驗證必得同一版面且穩定落在較優解(55),internals 加維護警示。②【熱點微優化,語意等價】cProfile 定位 conflicts() 佔 116/144s、_seg_cross 1150萬次呼叫、2 元素 sorted 2600萬次、abs 1.4億次——_seg_cross 方向判斷延遲計算(省近半 abs)、_seg_hits_box/_seg_cross/_wps_par_close/_wps_overlap 的 2 元素 sorted 全改條件交換;實測 63 節點雙頁案 46-50s→42s(約 -12%)。回歸:NSRRC 5 檔+5 範例 10 檔 md5 逐位一致、請假流程 validate 通過。③深層優化(decided 邊段預處理快取/空間索引)評估後暫緩:pairwise 幾何為 O(E²S²) 主成本,但重構三支 _wps_* 函式行為漂移風險高(前有 20260709.05 候選層並行語意漂移撤回之鑑),留待有大圖效能實需再議 |
| 20260710.10 | 2026-07-10 | 【輸出 bug 修復】.drawio cell id 撞 JS 內建屬性名致 draw.io 開檔失敗(使用者回報「文件加載出錯 d.setId is not a function」;請假流程圖節點 id="fill" 觸發):draw.io(mxGraph)以純 JS 物件/陣列作 id 查找表,id 與 Object/Array prototype 成員同名時查得內建函式而非圖形物件,開檔即炸。桌面版 CLI 逐一實測定位(二分法:框架格 OK→短 id 嫌疑組→僅改 fill 即癒),並以最小檔驗證 map/push/filter/sort/toString/constructor 全數開檔失敗、note1 正常,確立保留字集合=Object.prototype+Array.prototype+length。修三處:①bpmn_builder 新增 _JS_RESERVED(46 名),_ncname() 撞名者走既有「安全名_md5前6碼」確定性改名路徑 ②validate_bpmn 新增 JS_RESERVED_IDS 攔截(第二道防線,涵蓋使用者手改檔;與 builder 清單同步維護)③conventions id 規則節載明成因與「撰寫定義時直接避開」指引。回歸:請假流程圖重產後桌面版 CLI 匯出 PNG 成功、validate 全通過 |
| 20260710.09 | 2026-07-10 | 工件同泳道子欄標準(依使用者裁決:有工件之泳道直接加寬 ≥2 欄、工件盡量同泳道;程式 4 處+規範 3 處,含衝突規則移除):①新增 _artifact_widen——含工件夥伴的泳道 lane_subs 無條件 ≥2(不受 >12 列門檻,與 chain/wide 疊加取 max),於每個佈局候選內一體評分 ②_place_artifacts 重寫——候選序改「夥伴同泳道相鄰子欄(右優先,遠離左緣迴圈通道)→同泳道他列→最後手段鄰泳道」;原「跨泳道貼鄰優先」與「左鄰 backLoop 走廊懲罰」邏輯因根因消失整段移除 ③手動佈局豁免——僅工件省略 row 不再觸發整體自動佈局(_ensure 單 Proc 與 Collab need_auto 判定改流程節點限定),工件自動放置照走、明給 row 者尊重 ④conventions 刪「工件放置避讓迴圈通道(20260710.04)」整節(手工釘 lane 對策無效化),新增「工件同泳道子欄規則」節;「加寬只發生在自動佈局」改寫為選型加寬/工件加寬分立;手動佈局觸發條件同步改寫;internals 工件放置節重寫。回歸:5 範例(無工件)逐位一致、emit_multi 一致、20260710.04 情境(backLoop+工件不釘 lane)0 分無穿節點、手動+工件 row 保留、validate 全通過 |
| 20260710.08 | 2026-07-10 | 引擎層調整二項(依全面 REVIEW 裁決,程式 3 處+文件 1 處):①【Collab chain 缺口修復】_ensure 的 Collab 層級加寬加試迴圈原僅試 wide、未試 chain(20260709.04 新增 chain 時漏改 Collab 路徑,屬 silent 選型缺口)——補為 chain/wide 皆試,對齊單 Proc 路徑;含閘道長鏈 Collab 實測 chain 勝出 50→45 ②【並行移除】移除 emit_multi 按圖多進程並行(_render_pages 改串行迴圈、_emit_one_page 去 pickle)與 validate_bpmn 逐檔進程池、刪 BPMN_NO_PARALLEL;理由:20260710.03 已定調瓶頸全在流程方式非計算(5 圖 emit 0.39s),並行收益近零徒增維護面;workflow.md 刪引擎級並行段(作業級「並行作業」規則保留,兩者不同概念;候選層並行禁令背景留存於 20260709.05 條目)。回歸:5 範例輸出雙引擎逐位一致、emit_multi 雙圖專案逐位一致、純線性鏈 Collab 前後同分、validate 全通過 |
| 20260710.07 | 2026-07-10 | 迭代同步流程文件更新至 .drawio 預設時代(依全面 REVIEW 發現,純文件):workflow.md 迭代節六處補齊——①舊基準優先序改「.py > 上一版圖檔 XML(.drawio 或 .bpmn,依交付格式)> .md」(原僅列 .bpmn,與 .drawio 預設格式不一致) ②輸入來源路由補「.drawio → drawio_import.py」 ③第 1 步解析比對補 drawio_import diff 命令與「.py 基準先重產舊 .drawio 再 diff」指引 ④第 3 步 show 依格式對應工具 ⑤change_source 選項兩處補「使用者修改 .drawio」 ⑥節標題與 SKILL.md 迭代來源清單補 .drawio。程式碼零改動 |
| 20260710.06 | 2026-07-10 | workflow 效率規則微調(純文件):單次往返原則的「reference 必讀檔」原寫死三個檔名(workflow/conventions/example_process),改為描述性措辭「一條 cat 同時併讀必要讀取之檔案」——避免未來檔案增減時清單過時誤導。經評估後未採納「①階段逾時即違規」硬閾值(會與條件式查閱 BPMN 範例規則衝突、閾值無實證、屬事後判罰而非事前指引、與既有規則重複)。程式碼零改動 |
| 20260710.05 | 2026-07-10 | 端點選擇與箭頭方向規範同步(承 .04 一致性檢視,純文件):①conventions 評分表補「順序流缺方向」硬性項(檔案級、不計權重分,由 emitter+validate_bpmn 保證),並加註三方帳目說明(conventions 登記全部→check_layout 計 15 幾何項→validate 計幾何子集+箭頭硬性) ②conventions「避免重疊規則」新增第 10 條「端點選擇總則」:最近端優先→鄰近端→禁對向端,並載明使用者確認之因果(對向端為穿越自身節點與繞行過長之共同上游) ③internals.md 選路段補端點選擇機制、檢核參數段補箭頭方向三方帳目。經四方一致性檢視(conventions↔check_layout↔validate↔引擎)確認 15 評分項全對齊、無死 key、無 silent defect;端點禁對向端後果由既有穿越/繞行檢核覆蓋,不另設獨立檢核。程式碼零改動 |
| 20260710.04 | 2026-07-10 | 首產品質強化(依採購決標案實戰回饋自我學習,程式 1 處+檢核 1 處+規範 3 處):①【輸出 bug 修復】.drawio 順序流缺箭頭——_DIO_EDGE 樣式未顯式寫 endArrow,draw.io 對帶自訂 edgeStyle 的邊渲染成無方向線(使用者截圖回報);補 endArrow=block;endFill=1;startArrow=none(關連線維持 none、訊息流維持 open 不動);validate_bpmn 加檢核「順序流缺箭頭方向」(實線非 dashed 邊 style 須含 endArrow≠none),對修復前舊檔實測抓到 71 條、修復後 0 條 ②【表達力規範】conventions 閘道節新增「判斷語意必須自足」——菱形用疑問短語勿用動作簡稱、分支標籤具體成對、複雜條件前置判斷任務(解決「進入底價/接受議約」看不出在判斷什麼) ③【起手式規範】route 極簡原則新增「雙分支匯流起手式」例外——決標/廢標雙鏈匯流拓樸,決標主線閘道出邊給 sideLeft/sideRight 提示常翻盤全局(本輪 A-1 103→56) ④【工件避讓規範】新增「工件放置避讓迴圈通道」節——工件自動貼鄰換 lane 可能落在 backLoop 左通道造成穿節點,對策為明確釘 lane(本輪 A-2 決標記錄表釘 lane2)。5 範例回歸 0 分、箭頭 33 block+2 open、.bpmn 回讀通過 |
| 20260710.03 | 2026-07-10 | 流程提速(依使用者「全流程十幾分鐘不合理」回饋;實測引擎端到端 5 圖 emit 0.39s+validate 0.05s,瓶頸全在流程方式非計算):workflow.md 修訂——①手動微調預設 0 輪:builder 內建五輪自動修正已在 emit 內完成,validate 硬傷清零即交付,殘餘視覺分如實回報後直接交付(多數為拓樸極限,人工重試壓不掉);使用者要求再調以 2 輪為硬上限 ②改定義檔一律 str_replace 局部修改、禁止重寫全檔 ③新增「效率規則」節:reference 必讀檔單條 cat 併讀、定義檔執行與 validate 併一次 bash 呼叫 ④查例規則加註「先文字後圖、單次至多 2 頁」。預估全流程 10+ 分鐘→3–5 分鐘;程式碼零改動 |
| 20260710.02 | 2026-07-10 | 頁包二次裁減——純文字頁刪頁圖(依使用者「p.33 為何不能直接轉進 MD」之提議推廣):逐張目視 27 頁分類,6 張純文字頁(p.15/33/35/43/45/46,文字已完整在 MD)刪除頁圖、MD 引用改為「(純文字頁,無頁圖)」標記防維護誤判漏檔,檔頭查閱說明同步加註;其餘 21 頁含圖面(含第 7 章圖↔XML 序列化對照截圖)保留。頁包 2.9MB→2.2MB(-24%);引用/檔案 21/21 一致驗證通過;程式碼零改動 |
| 20260710.01 | 2026-07-10 | BPMN 2.0 by Example 頁包裁減(依使用者逐頁確認清單):移除與圖例無關 20 頁——前言/法務/目錄(p.01–10)、Incident Mgmt 執行版 XML 全文(p.22–26)、第 11 章 Correlation 整章(p.39–42)、Annex A(p.47);保留 27 頁關鍵圖例與範例說明(Ch5/6/7/8/9/10/12)。頁碼沿用原序、允許跳號(不重編避免動 27 個檔名與錨點);檔頭索引改為實際 p.NN 錨點頁碼(順帶修正原索引與頁序偏移 2 的錯誤)並加註裁減範圍。體積 4.5MB→2.9MB(-36%)、MD 88KB→48KB(-45%);MD 圖片引用與現存 JPEG 27/27 一一對應驗證通過;程式碼零改動 |
| 20260709.05 | 2026-07-09 | 繪製/驗證引擎級並行(依使用者指示):①emit_multi 按圖並行——新增 _emit_one_page worker + _render_pages 調度,各圖的佈局(_ensure)、drawio 頁 XML、SVG(含檢視器無補白版)、MD、語意/版面檢核整包丟獨立進程同時執行,母進程僅組檔;圖內演算法零改動,實測評分與串行逐字一致(A-1=38、A-2=23) ②validate_bpmn.py 逐檔並行——main 重構為 _check_one worker + 進程池,輸出行先收集後依原順序列印,新舊版對完整交付資料夾輸出 diff 為空 ③核心數自動閘門——兩者皆以 sched_getaffinity 偵測可用核心,單核自動退回串行零開銷(單核下多進程只增開銷:實測 2 圖 18.05s→19.01s);>1 核才啟用,失敗自動 fallback,BPMN_NO_PARALLEL=1 強制串行 ④維護警示入 workflow——勿改為佈局候選層並行:候選層需每候選各跑完整修正輪,實測 202s(反慢 11 倍)且選型語意漂移(A-1 38→48),已撤回 ⑤build_viewer_html_multi 增 svgs 參數供並行流程餵入預產 SVG。5 範例回歸 0 分、本案評分不變、validate 全通過 |
| 20260709.04 | 2026-07-09 | 採購決標案實戰回饋三修+二規(依使用者體檢決議):①database 佔格——NONGRID_TS 移除 "database",資料庫以順序流串入主線時佔格排入上下游之間(圓柱樣式與語意豁免不變),消除主線上繞;conventions 新增「資料庫節點佔格規則」節含維護提醒 ②多頁檢視器 defs id 隔離——build_viewer_html_multi 內嵌各頁 SVG 時 defs id 加頁碼前綴(p0_/p1_)並同步改寫 url(#…)/href 引用,修復「非首頁箭頭消失」(id 跨頁重複時瀏覽器解析到首頁 display:none 內的 marker 不繪製) ③chain 加寬模式(使用者提案)——僅「同泳道、一進一出、非閘道」節點橫向並排、多連線節點降列自佔一列;納入自動試算候選(列數>12 時 chain/wide 各以 2→3 子欄與單欄比評分取優、按圖各自決定),實測 A-2 46→23 分中選、A-1 落選維持單欄 ④emit_multi 跨圖節點 id 重複防護——重複即報錯提示加圖前綴(a1_/a2_);workflow 多流程圖節明文化 id 前綴規則 ⑤workflow 新增並行作業規則(多張圖單一定義檔一次 emit_multi 產齊、單次驗證,禁止逐張作業)與微調兩原則(證據式微調:改動必比評分、變差即撤;見好即收:硬傷清零、拓樸極限如實回報後停手);conventions 佈局模式節補 route 極簡原則 |
| 20260709.03 | 2026-07-09 | 規範↔檢核一致性補齊(依使用者體檢決議):①分區列範圍重疊入分 5 並納修正輪——_SCORE_RULES 與 BAD_KEYS 補「分區列範圍重疊」;_bad_nids 擴充分區名→成員節點反查(重疊訊息點名分區而非節點,原本產不出換位候選);實測計分驅動兩層防線:多版面試算即改選不重疊版面,拓樸可修時修正輪換位分離,成員與流向交錯(建模問題)則如實回報 5 分 ②conventions 補語意檢核小節(10 項判準+三條豁免明文化:terminate=合法終點、無 incoming 的 message/timer=觸發起點、工件/註解不參與語意檢查——防維護時誤當 bug 修掉) ③檢核表補「分區列範圍重疊」列、標籤列補「壓到節點/超出 pool 邊緣」例示 ④三格式對照表 .bpmn/.drawio 列補「交付完整性」、bands 節補檢核說明。5 範例+圖例V2回歸全 0 分 |
| 20260709.02 | 2026-07-09 | 全 skill 體檢瘦身(依使用者體檢決議,程式碼不動):①A/B/C 文件修正——conventions 規則編號重排為邏輯序(7-2d 移回 7-2 群、雙「8」解併為 8/9)、過時 7-2e「左邊距帶寬極限」併入 7-3a 側通道讓位敘述 ②F 資源瘦身——BPMN 2.0 by Example 由 6MB PDF 改為「頁包查閱版」reference/BPMN_2_0_by_Example.md + bpmn_pages/47 頁 JPEG(章節索引+逐頁文字+頁圖,grep 定位、只讀所需頁);CHANGELOG 保留最近 10 筆、其餘 24 筆歸檔至 CHANGELOG_archive.md ③E 文件分層——新增 reference/internals.md(佈局試算/選路/分軌/讓位軌/五輪修正等機制規格,僅維護時讀),conventions.md 只留寫定義檔所需慣例+檢核權重總表(單一來源,消除 7-x 內嵌分數與權重表的雙寫),19.9KB→13.7KB(-31%);SKILL.md 註明 internals 定位。產製必讀文件約省 3K tokens;5 範例回歸 0 分 |
| 20260709.01 | 2026-07-09 | 「加寬+換邊」納入修正候選,7-3a 已知極限收窄(依使用者「走左側不行嗎/泳道加寬可行嗎」之追問驗證):①側通道讓位——sideLeft 通道位原寫死泳道左緣+14,與 bndSide 讓位軌(+12 起)同廊互撞且用不到加寬空間,是「加寬救不回左側」根因;新增 _stash_bnd_tracks() 統計各泳道左廊讓位軌最深軌位(_bndL),sideLeft 通道外移 k×LINE_GAP 落於讓位軌之外(右緣 -14 vs -28 設計時已錯開,毋須處理);掛載於 _auto_routes 與 _ensure 手動路徑 ②修正輪組合候選——換位與加寬常須同時成立才降分(單試任一皆不改善而全數落選),每個換位候選加產「換位+加寬涉事泳道」組合(候選改四元組 widen_lane) ③試算前重設路線提示——auto_layout 快照原始 route 提示(_route_hints),_apply_and_route 每次試算重設回提示全量重選(沿用舊定案+逐條精修翻不出協同換邊的最佳分邊)。實測圖例V2協作圖:10(結構性交叉)→0;5 範例回歸全 0 分、.bpmn 檔案級驗證/回讀通過 |
| 20260708.27 | 2026-07-08 | 側通道巢狀與河道加寬(依使用者圖評):①分軌方向依目標深度定向——深者只許外移、淺者只許內移(深內淺外必生入段交叉);單向失敗即 fallback 改移對方(淺者內移) ②河道加寬機構 lane_pad——lane_width/節點置中支援每泳道加寬,五輪修正加入 widen 變異(+2×LINE_GAP),通道空間不足時自動加寬 ③精修插樁診斷確認殘餘一處為拓樸極限(扇出+長跨線共擠單側,深者出段必跨淺軌區間),記入 conventions。V2 維持 10(組成:僅此一條結構性交叉);全套回歸 0 分、0.3s |
| 20260708.26 | 2026-07-08 | 三條鐵則落地(使用者確認規格):①LINE_GAP=12 統一錯開常數(讓位軌/走廊/分軌共用);框線重疊檢核補「分區橫線」 ②最短最少彎——選路取優次序改(衝突, 轉折數, 路徑長),移除精修 cur==0 早退;新增「轉折過多」檢核(3 分,基準+1 內合格;慣例路線/分軌豁免) ③五輪修正 _repair_rounds():產出前若有交錯/重疊/間距/端口缺陷,對涉事節點嘗試換位(鄰列交換/移空位)+全量重佈線,擇優採納至多五輪,無改善即停並如實回報。V2:20→10(換位消一交叉,餘一條扇出交叉);5 範例/A-1/30 步/雙邊界窮舉/往返全綠,總耗時 0.4s |
| 20260708.25 | 2026-07-08 | 精修端口判定修復:.24 的邊界豁免使雙邊界窮舉退步 0→5(合流雙入共用終點端口)。根因:conflicts(skip) 的端口分支以 count>1 猜測「自己必占用該 key」,候選端點與舊端點不同時判定被錯誤放寬,導致版面變體間 straight↔sideLeft 振盪、最終停在撞端口的組合。修法:skip 時精確移除自己這條邊的舊端點再判 in——多入合流自動分散到 top/left/right 三端口。雙邊界窮舉(兩種深淺)回 0 分;V2 維持 20(兩條已知扇出/軌交叉);5 範例/A-1/30 步回歸全 0 分 |
| 20260708.24 | 2026-07-08 | 依使用者藍線軌跡修三項:①選路衝突函式鏡射「邊界貼宿主豁免」——宿主的順序流可直下出線(系統檢核→等待不再繞遠) ②邊界圓配置改「依目標方向分側」:左群貼左角、右群貼右角、群內目標淺者在外,底邊中央永遠讓出 ③關連線加「向上直達」候選(起訖預偏 +14 避開四正 port):正上方的註解走 65px 短徑,不再繞右側遠通道。V2 組成淨化(無重合/破口/標題壓線/貼行);5 範例/30 步/雙邊界窮舉回歸全 0 分 |
| 20260708.23 | 2026-07-08 | 依使用者洞察修二項:①讓位軌破口交叉根除——同宿主邊界圓的排列由 id 字串序改為「出邊目標」語意排序(目標在右者靠右;同向群內目標列淺者靠讓位側),幾何序等同目標深度序,交錯破口不再出現(採「交換逾時三日/檢核失敗位置」之洞察) ②容器標題壓線——hull 頂距 -34→-50(標題帶讓出折點帶)+ 新檢核「容器標題壓線」(3 分);分軌接受條件註記左邊距帶寬極限(36px 容通道+讓位軌,雙邊界+左向扇出並存時殘餘貼行由評分回報)。V2:20→18;5 範例/30 步/雙邊界窮舉回歸全 0 分 |
| 20260708.22 | 2026-07-08 | 依使用者圖評再修二項:①斷頭線——重合分軌的 12px 短橫移毛刺改為「平移併入鄰接水平段」(整段挪移;端點直出才插接段) ②藍線沿讓位軌貼行——重合判定距離 <1→≤2px、conflicts 加平行貼行(2~10px)×5 懲罰(選路端鏡像「間距不足」檢核),等待→收到回覆不再繞走讓位軌;新增「繞行過長」檢核(3 分:路徑 > 曼哈頓 ×2.2 且超出 >150px)。V2:30→20(殘餘 2 條=已記錄破口);5 範例/A-1/30 步/雙邊界窮舉回歸全 0 分 |
| 20260708.21 | 2026-07-08 | 依使用者圖評新增兩檢核點並加寬幾何:①平行間距檢核(5 分)——同向平行段 <10px 且並行 >30px 即報;讓位軌距 10→14、走廊錯開 12→14、分軌位移 7→12 配合加寬 ②框線重疊檢核(5 分)——連線與泳道邊界/pool 框/容器框可交錯穿越、不可沿線重疊(≤4px 貼行);容器 hull 齊格化(x 取泳道並集邊界內縮 6、y 頂 -34 避開折點帶)、分軌候選加框線避讓。實測 V2 紅框問題歸零(框線重疊 3→0、間距 0),殘餘 30 分皆屬已記錄破口家族;5 範例/A-1/30 步/雙邊界窮舉回歸全 0 分 |
| 20260708.20 | 2026-07-08 | 檢核強化三項(經確認清單)+ 重複檢核合併:①連線重合自動分軌——精修後仍共線的邊,共線垂直段平移 7px 短水平接回(wps_override 持久化,三輸出+檢核共用);_wps_overlap 排除端點 15px 樁段(與端口檢核劃清界線,消重複計分) ②端口重合正式檢核(5 分)——同節點多線共用進出點即報;關連線加「端口讓位」(撞點沿節點邊緣平移 14px)與流程邊端口懲罰 ③工件貼鄰——放置改(泳道×列距×子欄)扁平排序、貼鄰優先於指定泳道、夥伴有迴圈回入時左鄰墊底(避 backLoop 走廊);貼鄰格距 >1 警告(3 分) ④R1:關連線穿框併入統一邊集合(帶種類);訊息流穿框模型級改豁免(對齊 validator 慣例)。實測全型別 V2:38→20(端口重合修復、note/申請書貼鄰、重合分軌);5 範例/A-1/30 步/雙邊界窮舉/往返全數回歸 0 分 |
| 20260708.19 | 2026-07-08 | 邊界事件出邊讓位策略:新增專用路線 bndSide(固定慣例,不進候選試算與精修)——先落入列間水平走廊、沿泳道邊緣夾縫垂直下行、側面進入目標,避開宿主正下方扇出與既有側通道;同宿主多顆邊界依圓幾何序巢狀錯開(近讓位側=淺廊+外軌,A/B 實測優於目標深度序);_place 存 _bidx/_bmax、_auto_routes 預計算 _bg。實測全型別 V2 由 78→38(交叉 3→1、穿框 2→1),雙邊界窮舉(目標深淺兩種交錯)0 分;已知破口(遠側圓配淺目標的入段跨軌)記於 conventions,調 row 即解。內建 5 範例/A-1 型/30 步鏈回歸全 0 分 |
| 20260708.18 | 2026-07-08 | validate_bpmn 派工修正:單獨指定 .svg 檔時只跑 SVG 預覽檢查,不再誤入 BPMN 結構檢查(先前會誤報「找不到 bpmn:process」);已知優化項記錄:邊界事件出邊與宿主下方扇出共擠單一側通道時的讓位策略(全元素高密度圖的殘餘交叉來源) |
| 20260708.17 | 2026-07-08 | 圖例全部元素擴充(依 OMG 範例盤點、使用者決議全加):①事件體系——error/escalation/conditional/compensation 四型、kind=catch/throw(擲出實心)、中間事件自動雙圈(單圈=起訖)、邊界事件 attach=宿主(interrupting=False 非中斷虛線,貼宿主右下、深度隨宿主、合成邊納入拓撲鄰接防深度發散) ②事件型閘道 kind=event(雙圈五邊形→eventBasedGateway) ③活動——send/receive/script/call 四型(官方 taskMarker;call 粗框→callActivity)、loop=True 迴圈記號 ④工件——t=note 文字註解(→textAnnotation)、訊息流中點信封裝飾 ⑤容器 p.container(expanded/event;視覺 hull,.bpmn 以 Group 表示不做語意巢套) ⑥黑箱 pool add_blackbox(participant 無 processRef,可作訊息端點) ⑦三輸出/雙 validator/drawio_import 全面同步(drawio 事件 outline 動態化、邊界與宿主重疊豁免、訊息流穿框改提醒級);全型別 v2 往返斷言全過、內建 5 範例回歸 0 分 |
| 20260708.16 | 2026-07-08 | 修全型別示範圖的三個視覺問題:①工件被加寬分組/格位衝突推離夥伴(關連線長繞、看不出來源)——工件改為不參與分層網格,由 _place_artifacts() 以夥伴為圓心就近放置(同列相鄰→逐列外擴;手動指定 row 者尊重) ②同節點進出共用端口(進出同點)——conflicts() 加端口重用懲罰並記錄 used_ports ③先手邊與後手邊交叉無法避免(單輪貪婪盲區)——_auto_routes 加第二輪精修:全景已知下逐條重試候選(straight/side/enterRight),消除可避免的交叉。實測全型別圖 30 分→0 分;副效益:A-1 型不加寬即 10 列 0 缺陷;5 範例/30 步鏈/多頁/型別往返全數回歸通過 |
| 20260708.15 | 2026-07-08 | 組織 BPMN 繪製規範(圖例)落地與三輸出一致化補齊:①STYLE 設定層(draw.io 標準色盤)為單一來源;預設活動依決策改 generic(白) ②.drawio 端補齊——事件五型(下方標籤+message/timer 記號、terminate 黃圈)、活動四型(官方 task2+taskMarker+規範色)、工件(data2/datastore)、關連(dashPattern 點線) ③check_layout 標籤碰撞改「下方標籤」邏輯(事件+工件) ④check_semantics 工件豁免、terminate 視為合法終點、無 incoming 的 message/timer 視為觸發起點 ⑤validate_bpmn 補 subProcess/callActivity 標籤、工件 DI 檢查、association 端點納入穿框排除 ⑥關連線改 assoc_waypoints 衝突試算(三輸出共用)、閘道分支候選加 enterRight、builder 版面檢查納入關連線 ⑦drawio_import 認得全型別(task2 markers/data2/datastore/事件記號/黃圈中止)與關連,diff 增「改型」偵測 ⑧conventions.md 新增圖例對照表 |
| 20260708.14 | 2026-07-08 | 「主動加寬減列」名副其實:①「過長」計入選型評分——每超出 ROW_CAP(12)一列計 ROW_PENALTY(4)分,減列成為目標函數的一部分,加寬省 3 列以上即使多一條交叉仍中選 ②子欄逐級擴展:雙子欄壓完仍超標 → 試三子欄(MAX_SUBS=3),Proc 與 Collab 層級皆同 ③蛇行改牛耕式(偶數列左→右、奇數列右→左、換列垂直直下),修掉 (sub+1)%k 環繞跳欄橫穿中間欄的缺陷;加寬版分組保證格位唯一,泛用唯一迴圈於加寬版讓位(修溢出成員被過期 sub 疊成長串)。實測:30 步鏈 28→10 列(三子欄)、16 步鏈→8 列、A-1 型→8 列、Collab→8 列,全部 0 缺陷 |
| 20260708.13 | 2026-07-08 | 修使用者實測兩問題:①draw.io 拖檔報 d.insertEdge is not a function——.drawio 原本連線先於節點輸出,邊的 source/target 前向參照觸發部分版本的惰性解碼錯誤;改為節點在前、連線在後(draw.io 自存檔標準順序) ②鏈上夾閘道時加寬版壓不了列——wide 分層放寬為「僅閘道分支降列、其餘交接一律同列並排」,同泳道下行繼承子欄(垂直直下);wide 試算時原 route 提示(針對單欄版面)一律重設 auto 重新選路;閘道分支選路改衝突試算(慣例側出 vs 正交下行取衝突少者)。實測 A-1 型 14 列→8 列、0 缺陷、加寬中選 |
| 20260708.12 | 2026-07-08 | 依決議調整流程:圖檔 XML 預設改為 .drawio 且不再詢問使用者(emit 預設 fmt="drawio";僅使用者明講要用 bpmn.io 時才 fmt="bpmn");SKILL.md 與 workflow.md 交付表/規則同步改寫;內建 5 範例以新預設回歸全過(產 .drawio) |
| 20260708.11 | 2026-07-08 | 會議三決議落地:①多流程圖一檔——build_drawio 重構抽出 _drawio_page_xml,新增 build_drawio_multi(多 <diagram> 頁籤)與 emit_multi 專案級交付({專案}_{版號}.drawio + 多頁檢視器(頁籤切換、各頁獨立縮放/搜尋)+ 各圖 SVG/MD + 專案層級版本記錄,版號以專案為準);validate 的 check_drawio 改逐頁檢查、交付完整性支援多頁檔 ②人工微調整合——新增 drawio_import.py(show 重建定義/diff 差異),節點吸附回格線(lane/sub 由泳道框反查、row 反算四捨五入),改名/挪位=文字、增刪=結構,往返測試型別/格位/kind 完全一致 ③繪製規範 style 設定層待規範文件或示範檔提供後落地 |
| 20260708.10 | 2026-07-08 | 超長圖治本:自動佈局新增「加寬版」第三變體——緊湊/傳統版最佳仍 >12 列時,把長鏈所在泳道加寬為雙子欄(lane_subs),同泳道任務→任務交接同列並排、子欄交替蛇行,列數近乎減半(實測 16 步鏈 16 列→9 列、0 缺陷);座標模型引入 node.sub 與 Proc.lane_subs,pool_width/_lane_x/側通道 cx/三種輸出泳道寬度全面連動;仍以 layout_score 評分取優,短圖不受影響 |
| 20260708.09 | 2026-07-08 | 三格式檢核一致化稽核:①發現語意檢查(懸空/孤兒/可達性/start-end 數/閘道規則/訊息流跨 pool)只在 .bpmn 檔案端,fmt=drawio 完全不跑——上移為模型層 check_semantics(x),emit 對兩種 fmt 一律執行並列印;.bpmn 檔案級同類檢查保留為手改檔第二道防線 ②check_drawio 補幾何再驗(節點重疊、waypoint 邊穿框;排除 dio_ 框架 cell),對齊 .bpmn 的 DI 再驗 ③emit 回傳改為語意+版面問題合併清單 ④conventions.md 新增三格式檢核對照表 |
| 20260708.08 | 2026-07-08 | 修 bpmn.io 回報的兩問題:①同列跨欄交接的走線——_orth_down 原以「出底邊→中線水平→上折進頂邊」畫,於 bpmn.io/draw.io 顯示為線貫穿節點內部+向上箭頭;改為兩節點中心同高時走「側到側單段水平線」(三種輸出共用 waypoints,一次修齊) ②.bpmn 的泳道 DI 未填滿 pool,左緣留 30px 標頭縫(SVG 左直條在 bpmn.io 無用途);第一泳道向左延伸吃掉縫、貼齊 pool 左緣 |
| 20260708.07 | 2026-07-08 | 三種輸出視覺一致化:①.bpmn 為 start/end 事件補 BPMNLabel DI(≤4 字置圈內、>4 字置圓右側,與 SVG 同規則;原先 bpmn.io 自動放圈外) ②.bpmn 的 pool DI 上緣加 36px 頂帶,參與者名稱不再壓到中間泳道標頭 ③.drawio 事件標籤改圈內/右側兩版樣式(移除下方標籤)、新增圖頂橫式標題(name+版號)與泳道頭底線、bands 時左直條省 pool 名 |
| 20260708.06 | 2026-07-08 | 自動佈局分層改雙版面試算(--tune 模式):緊湊版讓跨欄順序交接(來源非閘道)同列並排、壓低總列數消除樓梯狀長圖;傳統版保留給迴圈/合流情境。兩版各排一輪以 layout_score 取優(同分取列數少);Collab 於整體層級試算以涵蓋跨 pool 訊息流;分層/格位衝突改定點迭代避免向上邊;拆出 _check_placed() 供試算呼叫避免遞迴;每輪試算前還原 flows 快照避免 route 汙染 |
| 20260708.05 | 2026-07-08 | 補白規則封頂:直式細長圖補白至寬高比 1.3 時,新增上限「補白後寬 ≤ 內容寬 × 2」(主圖至少佔畫布一半),超長圖不再被稀釋成細條;封頂生效時 SVG 根元素標 data-pad="capped",validate_bpmn.py 對帶標記者將寬高比檢查由 ✗ 降為 • 提醒(引導以 _檢視器.html 細看),無標記之低比例 SVG 維持嚴格 FAIL;conventions.md viewBox 節同步 |
| 20260708.04 | 2026-07-08 | 全面 review 簡化(對照 drawio-skill 實務):①消除文件與程式不一致——builder 模組 docstring 補 fmt/build_drawio/layout_score/bands 與 id 雙軌、validate_bpmn 檔頭補 --score 與 .drawio 檢查與交付擇一、example_process 檔頭同步 ②去重——SKILL.md 交付細節收斂為指向 workflow.md、版號規則四處重複收斂為 workflow.md 單一來源、conventions「一律正交」併入避免重疊規則、pool 高度公式三處重複抽為 _pool_height() ③修不合理——emit/_write_changelog 局部變數 xid 更名 stem(消除與 XML xid 屬性同名異義)、build_drawio 空流程補 default=0、評分「兩端權重一致」措辭修正為「權重表共用、涵蓋類別不同」 |
| 20260708.03 | 2026-07-08 | 圖檔 XML 改為使用者擇一:`emit(x, fmt="bpmn"|"drawio")` 二選一產出(預設 bpmn;確認需求時先問要用哪個工具、明講即不問),取代 .02 的選配第 7 檔設計 ②validate_bpmn.py 同步支援:蒐集 .drawio、新增 `check_drawio()` 結構檢查(根節點 0/1、id 唯一、懸空引用、自閉合邊)、交付完整性改為兩格式擇一判定 ③emit 韌性:src 定義檔不存在時警告跳過而非半途中斷 ④SKILL.md 與 workflow.md 交付表/確認流程同步改寫;.drawio 路徑不支援回讀迭代(以 .md/圖檔/文字迭代)已註明 |
| 20260708.02 | 2026-07-08 | 借鏡 drawio-skill 迭代:①新增 `build_drawio()` 原生 draw.io(mxGraph)匯出與 `emit(drawio=True)` 選配第 7 檔——draw.io 不支援 BPMN 2.0 XML,.bpmn 丟入必跑版,此為正解;形狀取自 draw.io 官方 mxgraph.bpmn 索引、座標/waypoint 與 SVG 完全一致 ②新增版面可讀性評分 `layout_score()`(穿越20/斜線15/交叉10/重合8/重疊5/標籤3)並整合進 emit 輸出與 `validate_bpmn.py --score`,供版面變體量化比較 ③檢視器新增節點文字搜尋(高亮 + Enter 循環置中)④workflow.md 記載 draw.io/bpmn.io 格式不相容事實與交付指引 |
| 20260708.01 | 2026-07-08 | 修復 bpmn.io 匯入失敗(illegal ID):XML 內 id 一律清洗為 ASCII NCName。①新增 `_ncname()`(非法字元轉底線 + md5 前 6 碼保唯一、確定性映射) ②`pid`/`cid` 雙軌:原字串續用於檔名/顯示(可中文),XML 改用 `xid` ③節點 id 於 `add`/`flow`/`message`/`bands` 建模時即清洗,引用自動一致 ④validate_bpmn.py 新增全檔 id NCName 檢查 ⑤conventions.md 補「id ASCII、中文放 name」約定 |
| 20260706.05 | 2026-07-06 | 基準版:自動佈局、側通道繞線與交叉避讓、檢視器修正、圖面版號規則(V主版.次版)等歷次改進(此前未逐版記錄) |
| 20260707.01 | 2026-07-07 | ①交付物固定 6 檔(新增 HTML 檢視器常備、_流程定義.py、獨立版本記錄表) ②新增 bpmn_import.py:解析使用者修改後的 .md/.bpmn、差異比對、版號進位建議,支援迭代同步 ③工作流程改為「確認先行」並支援會議記錄輸入(多流程先列清單挑選) ④SKILL.md 瘦身,流程/命名/版號規則移至 reference/workflow.md ⑤validate_bpmn.py 加交付完整性檢查 ⑥新增本 CHANGELOG |
| 20260707.02 | 2026-07-07 | workflow.md 新增「資訊缺漏處理」明文規則(角色缺漏→推斷+假設或單泳道二擇一;步驟歸屬不明→標註假設;分支缺漏→列建議調整),首次繪圖與迭代皆須加列建議調整並詢問 |
| 20260707.03 | 2026-07-07 | workflow.md 產出步驟新增條件式規格查閱規則:builder 慣例未涵蓋的建模情境(附加事件、事件子流程、閘道取捨疑義等)須先查閱 BPMN_2_0_by_Example.pdf 確認建模方式再進版;一般案件以 validate_bpmn.py 通過為合規門檻(首次繪圖與迭代皆適用) |
| 20260707.04 | 2026-07-07 | 迭代輸入來源擴充為四種:圖檔、.md、.bpmn、描述文字;workflow.md 明訂各來源比對方式(.md/.bpmn 用 bpmn_import.py 自動比對,圖檔由 Claude 判讀差異,描述文字整理成變更清單),change_source 增列「使用者修改圖檔」 |
| 20260707.05 | 2026-07-07 | 全面 review 修正:①emit 未填 change/change_source 改為可見佔位+終端警告(不再默默填「口頭指示」) ②資訊缺漏處理輸入列舉補圖檔/描述文字 ③明訂圖檔/描述來源由 Claude 判斷結構/文字分類後以 bump_version 建議版號 ④明訂迭代舊基準優先序(.py>.bpmn>.md) ⑤conventions 檢視器節改為固定交付措辭、驗證節補交付完整性 ⑥builder docstring 移除不準確的「皆相容舊版」 ⑦workflow 首次繪圖驗證細節去重指向 conventions ⑧SKILL.md 刪 emit 參數式重複 ⑨命名規則補 pid(檔名)/name(顯示)分工 |
